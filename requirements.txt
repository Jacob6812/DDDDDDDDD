# DarwinTrade + StockBench runtime dependencies.
#
# This file intentionally lists direct project dependencies instead of a full
# `pip freeze` snapshot. Let pip resolve transitive dependencies so we do not
# lock incompatible subdependency versions across platforms.

# Core data stack
numpy>=2.0,<3.0
pandas>=2.2,<3.0
scipy>=1.11,<2.0
pyarrow>=15.0,<25.0

# Validation, config, CLI
pydantic>=2.8,<3.0
python-dotenv>=1.0,<2.0
PyYAML>=6.0,<7.0
typer>=0.12,<1.0
rich>=13.0,<16.0
loguru>=0.7,<1.0
tzdata>=2024.1
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0

# LLM and agent orchestration
openai>=2.0,<3.0
langchain-core>=1.0,<2.0
langchain-openai>=1.0,<2.0
langgraph>=1.0,<2.0

# Optimization and portfolio research
cvxpy>=1.6,<2.0

# Market data and StockBench integrations
httpx>=0.27,<1.0
finnhub-python>=2.4,<3.0
yfinance>=0.2.50,<2.0
akshare>=1.15,<2.0
json-repair>=0.30,<1.0
demjson3>=3.0,<4.0
beautifulsoup4>=4.12,<5.0

# Reporting and optional built-in StockBench strategy families
matplotlib>=3.8,<4.0
statsmodels>=0.14,<1.0
xgboost>=2.0,<4.0

# Test/development tools used by this repository
pytest>=8.0,<9.0
pytest-cov>=5.0,<8.0
